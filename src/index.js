//react dom  = modelo de objeto de documento
import ReactDOM from'react-dom'

const el = document.getElementById('root') // pega a div root do html e passa como parametro
ReactDOM.render('Olá React',el)